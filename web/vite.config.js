import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  base: '/Steganography-Tool-v1.0.0/',
  build: {
    outDir: 'dist',
    assetsDir: 'assets'
  }
})
